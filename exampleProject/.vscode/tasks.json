{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "debug build",
            "type": "shell",
            "command": "cd build && rm CMakeCache.txt && cmake .. && make -j$(nproc)"
        }
    ]
}
